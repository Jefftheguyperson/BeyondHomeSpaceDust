// Defines Argon Gas definitions for Beyond Home planets.



// Fury
// =============
SPACEDUST_RESOURCE
{
  resourceName = ArgonGas
  body = Fury
  
  RESOURCEBAND
  {
    name = furyAtmo
    title = #LOC_SpaceDust_Band_Atmosphere
      
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // The lower this number, the longer the band will take to discover with a Remote telescope
    remoteDiscoveryScale = 0.75

    // Abundance Data
    // --------------
    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.0000013
    maxAbundance = 0.0000014


    // Scale abundances by air density
    useAirDensity = True
    // 
    densityCurve
    {
        key = 0 0
        key = 1 1
        key = 12 12
    }

    // Distribution Data
    // --------------

    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 80000
    // minimum altitude for a distribution
    altLowerBound = -500
    // peak of a distribution
    altPeak = 0
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}


// Hydrus
// =============
SPACEDUST_RESOURCE
{
  resourceName = ArgonGas
  body = Hydrus
  
  RESOURCEBAND
  {
    name = hydrusAtmo
    title = #LOC_SpaceDust_Band_Atmosphere
      
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Abundance Data
    // --------------
    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.00000041
    maxAbundance = 0.00000042

    // Scale abundances by air density
    useAirDensity = True
    // 
    densityCurve
    {
        key = 0 0
        key = 1 1
        key = 12 12
    }

    // Distribution Data
    // --------------

    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 50000
    // minimum altitude for a distribution
    altLowerBound = -500
    // peak of a distribution
    altPeak = 0
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}


// Rhode
// =============
@SPACEDUST_RESOURCE:HAS[#body[Kerbin],#resourceName[ArgonGas]]
{
  @RESOURCEBAND[kerbinAtmo]
  {
    @name = rhodeAtmo
    title = #LOC_SpaceDust_Band_Atmosphere
      
    // Discoverability Data
    // --------------
    @alwaysDiscovered = true
    @alwaysIdentified = true

    // Abundance Data
    // --------------
    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    @minAbundance = 0.000010
    @maxAbundance = 0.000011

    // Scale abundances by air density
    useAirDensity = True
    // 
    densityCurve
    {
        key = 0 0
        key = 1 1
        key = 12 12
    }

    // Distribution Data
    // --------------

    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    @altUpperBound = 50000
    // minimum altitude for a distribution
    @altLowerBound = -500
    // peak of a distribution
    @altPeak = 0
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}


// Lua
// =============
SPACEDUST_RESOURCE
{
  resourceName = ArgonGas
  body = Lua
  
  RESOURCEBAND
  {
    name = luaAtmo
    title = #LOC_SpaceDust_Band_Atmosphere
      
    // Discoverability Data
    // --------------
    alwaysDiscovered = true
    alwaysIdentified = false

    // Abundance Data
    // --------------
    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.000000010
    maxAbundance = 0.000000011

    // Scale abundances by air density
    useAirDensity = True
    // 
    densityCurve
    {
        key = 0 0
        key = 1 1
        key = 12 12
    }

    // Distribution Data
    // --------------

    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 10000
    // minimum altitude for a distribution
    altLowerBound = -500
    // peak of a distribution
    altPeak = 0
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}


// Gateway
// =============
SPACEDUST_RESOURCE
{
  resourceName = ArgonGas
  body = Gateway
  
  RESOURCEBAND
  {
    name = gatewayAtmo
    title = #LOC_SpaceDust_Band_Atmosphere
      
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Abundance Data
    // --------------
    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.000011
    maxAbundance = 0.000012

    // Scale abundances by air density
    useAirDensity = True
    // 
    densityCurve
    {
        key = 0 0
        key = 1 1
        key = 12 12
    }

    // Distribution Data
    // --------------

    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 240000
    // minimum altitude for a distribution
    altLowerBound = -500
    // peak of a distribution
    altPeak = 0
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}


// Kohm
// =============
SPACEDUST_RESOURCE
{
  resourceName = ArgonGas
  body = Kohm
  
  RESOURCEBAND
  {
    name = kohmAtmo
    title = #LOC_SpaceDust_Band_Atmosphere
      
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Abundance Data
    // --------------
    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.000010
    maxAbundance = 0.000011

    // Scale abundances by air density
    useAirDensity = True
    // 
    densityCurve
    {
        key = 0 0
        key = 1 1
        key = 12 12
    }

    // Distribution Data
    // --------------

    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 50000
    // minimum altitude for a distribution
    altLowerBound = -500
    // peak of a distribution
    altPeak = 0
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}


// Vasto
// =============
SPACEDUST_RESOURCE
{
  resourceName = ArgonGas
  body = Vasto
  
  RESOURCEBAND
  {
    name = vastoAtmo
    title = #LOC_SpaceDust_Band_Atmosphere
      
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Abundance Data
    // --------------
    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.00000040
    maxAbundance = 0.00000041

    // Scale abundances by air density
    useAirDensity = True
    // 
    densityCurve
    {
        key = 0 0
        key = 1 1
        key = 12 12
    }

    // Distribution Data
    // --------------

    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 35000
    // minimum altitude for a distribution
    altLowerBound = -500
    // peak of a distribution
    altPeak = 0
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}


// Dynasty
// =============
SPACEDUST_RESOURCE
{
  resourceName = ArgonGas
  body = Dynasty
  
  RESOURCEBAND
  {
    name = dynastyAtmo
    title = #LOC_SpaceDust_Band_Atmosphere
      
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Abundance Data
    // --------------
    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.00000038
    maxAbundance = 0.00000040

    // Scale abundances by air density
    useAirDensity = True
    // 
    densityCurve
    {
        key = 0 0
        key = 1 1
        key = 12 12
    }

    // Distribution Data
    // --------------

    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 50000
    // minimum altitude for a distribution
    altLowerBound = -500
    // peak of a distribution
    altPeak = 0
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}


// Janus
// =============
SPACEDUST_RESOURCE
{
  resourceName = ArgonGas
  body = Janus
  
  RESOURCEBAND
  {
    name = janusAtmo
    title = #LOC_SpaceDust_Band_Atmosphere
      
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Abundance Data
    // --------------
    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.00000000081
    maxAbundance = 0.00000000082

    // Scale abundances by air density
    useAirDensity = True
    // 
    densityCurve
    {
        key = 0 0
        key = 1 1
        key = 12 12
    }

    // Distribution Data
    // --------------

    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 110000
    // minimum altitude for a distribution
    altLowerBound = -500
    // peak of a distribution
    altPeak = 0
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}


// Tau
// =============
SPACEDUST_RESOURCE
{
  resourceName = ArgonGas
  body = Tau
  
  RESOURCEBAND
  {
    name = tauAtmo
    title = #LOC_SpaceDust_Band_Atmosphere
      
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Abundance Data
    // --------------
    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.000030
    maxAbundance = 0.000031

    // Scale abundances by air density
    useAirDensity = True
    // 
    densityCurve
    {
        key = 0 0
        key = 1 1
        key = 12 12
    }

    // Distribution Data
    // --------------

    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 50000
    // minimum altitude for a distribution
    altLowerBound = -500
    // peak of a distribution
    altPeak = 0
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}


// Eterna
// =============
SPACEDUST_RESOURCE
{
  resourceName = ArgonGas
  body = Eterna
  
  RESOURCEBAND
  {
    name = eternaAtmo
    title = #LOC_SpaceDust_Band_Atmosphere
      
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Abundance Data
    // --------------
    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.00000090
    maxAbundance = 0.00000091

    // Scale abundances by air density
    useAirDensity = True
    // 
    densityCurve
    {
        key = 0 0
        key = 1 1
        key = 12 12
    }

    // Distribution Data
    // --------------

    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 25000
    // minimum altitude for a distribution
    altLowerBound = -500
    // peak of a distribution
    altPeak = 0
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}


// Verna
// =============
SPACEDUST_RESOURCE
{
  resourceName = ArgonGas
  body = Verna
  
  RESOURCEBAND
  {
    name = vernaAtmo
    title = #LOC_SpaceDust_Band_Atmosphere
      
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Abundance Data
    // --------------
    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.00000050
    maxAbundance = 0.00000051

    // Scale abundances by air density
    useAirDensity = True
    // 
    densityCurve
    {
        key = 0 0
        key = 1 1
        key = 12 12
    }

    // Distribution Data
    // --------------

    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 30000
    // minimum altitude for a distribution
    altLowerBound = -500
    // peak of a distribution
    altPeak = 0
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}