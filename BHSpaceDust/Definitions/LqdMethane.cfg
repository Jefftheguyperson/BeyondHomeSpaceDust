// Defines Methane Distribution for Beyond Home.


// Hydrus
// =============
SPACEDUST_RESOURCE
{
  resourceName = LqdMethane
  body = Hydrus
  
  RESOURCEBAND
  {
    name = hydrusAtmo
    title = #LOC_SpaceDust_Band_Atmosphere
      
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Abundance Data
    // --------------
    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.00011
    maxAbundance = 0.00011

    // Scale abundances by air density
    useAirDensity = True
    // 
    densityCurve
    {
        key = 0 0
        key = 1 1
        key = 12 12
    }

    // Distribution Data
    // --------------

    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 50000
    // minimum altitude for a distribution
    altLowerBound = -500
    // peak of a distribution
    altPeak = 0
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}

// Rhode
// =============
SPACEDUST_RESOURCE
{
  resourceName = LqdMethane
  body = Kerbin
  
  RESOURCEBAND
  {
    name = rhodeAtmo
    title = #LOC_SpaceDust_Band_Atmosphere
      
    // Discoverability Data
    // --------------
    alwaysDiscovered = true
    alwaysIdentified = true

    // Abundance Data
    // --------------
    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.0000000022
    maxAbundance = 0.0000000022

    // Scale abundances by air density
    useAirDensity = True
    // 
    densityCurve
    {
        key = 0 0
        key = 1 1
        key = 12 12
    }

    // Distribution Data
    // --------------

    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 50000
    // minimum altitude for a distribution
    altLowerBound = -500
    // peak of a distribution
    altPeak = 0
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}

// Lua
// =============
SPACEDUST_RESOURCE
{
  resourceName = LqdMethane
  body = Lua
  
  RESOURCEBAND
  {
    name = LuaAtmo
    title = #LOC_SpaceDust_Band_Atmosphere
      
    // Discoverability Data
    // --------------
    alwaysDiscovered = true
    alwaysIdentified = false

    // Abundance Data
    // --------------
    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.000000000022
    maxAbundance = 0.000000000022

    // Scale abundances by air density
    useAirDensity = True
    // 
    densityCurve
    {
        key = 0 0
        key = 1 1
        key = 12 12
    }

    // Distribution Data
    // --------------

    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 10000
    // minimum altitude for a distribution
    altLowerBound = -500
    // peak of a distribution
    altPeak = 0
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}

// Gateway
// =============
SPACEDUST_RESOURCE
{
  resourceName = LqdMethane
  body = Gateway
  
  RESOURCEBAND
  {
    name = gatewayAtmo
    title = #LOC_SpaceDust_Band_Atmosphere
      
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Abundance Data
    // --------------
    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.000000022
    maxAbundance = 0.000000022

    // Scale abundances by air density
    useAirDensity = True
    // 
    densityCurve
    {
        key = 0 0
        key = 1 1
        key = 12 12
    }

    // Distribution Data
    // --------------

    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 240000
    // minimum altitude for a distribution
    altLowerBound = -500
    // peak of a distribution
    altPeak = 0
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}

// Kohm
// =============
SPACEDUST_RESOURCE
{
  resourceName = LqdMethane
  body = Kohm
  
  RESOURCEBAND
  {
    name = kohmAtmo
    title = #LOC_SpaceDust_Band_Atmosphere
      
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Abundance Data
    // --------------
    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.0000000022
    maxAbundance = 0.0000000022

    // Scale abundances by air density
    useAirDensity = True
    // 
    densityCurve
    {
        key = 0 0
        key = 1 1
        key = 12 12
    }

    // Distribution Data
    // --------------

    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 50000
    // minimum altitude for a distribution
    altLowerBound = -500
    // peak of a distribution
    altPeak = 0
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}

// Dynasty
// =============
SPACEDUST_RESOURCE
{
  resourceName = LqdMethane
  body = Dynasty
  
  RESOURCEBAND
  {
    name = dynastyAtmo
    title = #LOC_SpaceDust_Band_Atmosphere
      
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Abundance Data
    // --------------
    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.0000000008
    maxAbundance = 0.0000000008

    // Scale abundances by air density
    useAirDensity = True
    // 
    densityCurve
    {
        key = 0 0
        key = 1 1
        key = 12 12
    }

    // Distribution Data
    // --------------

    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 50000
    // minimum altitude for a distribution
    altLowerBound = -500
    // peak of a distribution
    altPeak = 0
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}

// Janus
// =============
SPACEDUST_RESOURCE
{
  resourceName = LqdMethane
  body = Janus
  
  RESOURCEBAND
  {
    name = JanusAtmo
    title = #LOC_SpaceDust_Band_Atmosphere
      
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Abundance Data
    // --------------
    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.001978
    maxAbundance = 0.001978

    // Scale abundances by air density
    useAirDensity = True
    // 
    densityCurve
    {
        key = 0 0
        key = 1 1
        key = 12 12
    }

    // Distribution Data
    // --------------

    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 200000
    // minimum altitude for a distribution
    altLowerBound = -500
    // peak of a distribution
    altPeak = 0
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}

// Tau
// =============
SPACEDUST_RESOURCE
{
  resourceName = LqdMethane
  body = Tau
  
  RESOURCEBAND
  {
    name = TauAtmo
    title = #LOC_SpaceDust_Band_Atmosphere
      
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Abundance Data
    // --------------
    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.00000000082
    maxAbundance = 0.00000000082

    // Scale abundances by air density
    useAirDensity = True
    // 
    densityCurve
    {
        key = 0 0
        key = 1 1
        key = 12 12
    }

    // Distribution Data
    // --------------

    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 50000
    // minimum altitude for a distribution
    altLowerBound = -500
    // peak of a distribution
    altPeak = 0
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}