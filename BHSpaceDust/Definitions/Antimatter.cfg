// Defines Antimatter definitions for Beyond Home planets.

// Fury
// =============
SPACEDUST_RESOURCE
{
  resourceName = Antimatter
  body = Fury
  RESOURCEBAND
  {
    name = furyRing
    title = #LOC_SpaceDust_Band_LowBelt

    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
       minAbundance = 4.1E-28
       maxAbundance = 1.2E-27


    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 650000
    // minimum altitude for a distribution
    altLowerBound = 550000
    // peak of a distribution
    altPeak = 600000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 10
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear
    altitudeSquish = 0.9
    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 40
    // Minimum latitude for a distribution
    latLowerBound = -40
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 1
    // Falloff for latitude, can be Linear or None
    latFalloffType = Linear
  }

  RESOURCEBAND
  {
    name = furyRingHigher
    title = #LOC_SpaceDust_Band_HighBelt

    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 8.1E-28
    maxAbundance = 2.4E-27

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 2100000
    // minimum altitude for a distribution
    altLowerBound = 1900000
    // peak of a distribution
    altPeak = 2000000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 10
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear
    altitudeSquish = 1

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 55
    // Minimum latitude for a distribution
    latLowerBound = -55
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 1
    // Falloff for latitude, can be Linear or None
    latFalloffType = Linear
  }

}


// Hydrus
// =============
SPACEDUST_RESOURCE
{
  resourceName = Antimatter
  body = Hydrus
  RESOURCEBAND
  {
    name = hydrusRing
    title = #LOC_SpaceDust_Band_LowBelt

    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
       minAbundance = 1.1E-28
       maxAbundance = 8.2E-28


    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 450000
    // minimum altitude for a distribution
    altLowerBound = 350000
    // peak of a distribution
    altPeak = 400000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 10
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear
    altitudeSquish = 0.9
    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 40
    // Minimum latitude for a distribution
    latLowerBound = -40
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 1
    // Falloff for latitude, can be Linear or None
    latFalloffType = Linear
  }

  RESOURCEBAND
  {
    name = hydrusRingHigher
    title = #LOC_SpaceDust_Band_HighBelt

    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 4.1E-28
    maxAbundance = 8.4E-28

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 1900000
    // minimum altitude for a distribution
    altLowerBound = 1700000
    // peak of a distribution
    altPeak = 1800000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 10
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear
    altitudeSquish = 1

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 55
    // Minimum latitude for a distribution
    latLowerBound = -55
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 1
    // Falloff for latitude, can be Linear or None
    latFalloffType = Linear
  }

}


// Rhode
// =============
@SPACEDUST_RESOURCE:HAS[#body[Kerbin],#resourceName[Antimatter]]
{
  @RESOURCEBAND[kerbRing]
  {
    @name = rhodeRing
    title = #LOC_SpaceDust_Band_LowBelt

    // Discoverability Data
    // --------------
    @alwaysDiscovered = true
    @alwaysIdentified = false

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
       @minAbundance = 4.0E-29
       @maxAbundance = 1.1E-28


    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    @altUpperBound = 650000
    // minimum altitude for a distribution
    @altLowerBound = 550000
    // peak of a distribution
    @altPeak = 600000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 10
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear
    altitudeSquish = 0.9
    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 40
    // Minimum latitude for a distribution
    latLowerBound = -40
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 1
    // Falloff for latitude, can be Linear or None
    latFalloffType = Linear
  }

  @RESOURCEBAND[kerbRingHigher]
  {
    @name = rhodeRingHigher
    title = #LOC_SpaceDust_Band_HighBelt

    // Discoverability Data
    // --------------
    @alwaysDiscovered = true
    @alwaysIdentified = false

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    @minAbundance = 8.0E-29
    @maxAbundance = 2.3E-28

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    @altUpperBound = 2100000
    // minimum altitude for a distribution
    @altLowerBound = 1900000
    // peak of a distribution
    @altPeak = 2000000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 10
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear
    altitudeSquish = 1

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 55
    // Minimum latitude for a distribution
    latLowerBound = -55
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 1
    // Falloff for latitude, can be Linear or None
    latFalloffType = Linear
  }

}


// Gateway
// =============

SPACEDUST_RESOURCE
{
  resourceName = Antimatter
  body = Gateway

  RESOURCEBAND
  {
    name = gatewayExo
    title = #LOC_SpaceDust_Band_LowBelt

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.00000000000000000000000000000061
    maxAbundance = 0.00000000000000000000000000000421

    // Visuals Data
    // -------------
    countScale = 2
    rotateRate = 1
    
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 60000000
    // minimum altitude for a distribution
    altLowerBound = 40000000
    // peak of a distribution
    altPeak = 5000000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 25
    // Minimum latitude for a distribution
    latLowerBound = -25
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 6
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }

  RESOURCEBAND
  {
    name = kohmAreaRing
    // Note: Probably should make sure the ring is actually at the height of Kohm...

    title = #LOC_SpaceDust_Band_Ring

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.0000000000000000000000000000011
    maxAbundance = 0.0000000000000000000000000000041

     // Visuals Data
    // -------------
    countScale = 20
    rotateRate = 0.25

    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 23000000
    // minimum altitude for a distribution
    altLowerBound = 21000000
    // peak of a distribution
    altPeak = 22000000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 2
    // Minimum latitude for a distribution
    latLowerBound = -2
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }

}


// Kohm
// =========

SPACEDUST_RESOURCE
{
  resourceName = Antimatter
  body = Kohm
  RESOURCEBAND
  {
    name = laytheRing
    title = #LOC_SpaceDust_Band_LowBelt

    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.0000000000000000000000000000041
    maxAbundance = 0.0000000000000000000000000000121

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 2550000
    // minimum altitude for a distribution
    altLowerBound = 500000
    // peak of a distribution
    altPeak = 600000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 10
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear
    altitudeSquish = 0.9
    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 40
    // Minimum latitude for a distribution
    latLowerBound = -40
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 1
    // Falloff for latitude, can be Linear or None
    latFalloffType = Linear
  }

}


// Vasto
// =============
SPACEDUST_RESOURCE
{
  resourceName = Antimatter
  body = Vasto
  RESOURCEBAND
  {
    name = vastoRing
    title = #LOC_SpaceDust_Band_LowBelt

    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
       minAbundance = 3.1E-30
       maxAbundance = 1.0E-29


    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 450000
    // minimum altitude for a distribution
    altLowerBound = 350000
    // peak of a distribution
    altPeak = 400000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 10
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear
    altitudeSquish = 0.9
    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 40
    // Minimum latitude for a distribution
    latLowerBound = -40
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 1
    // Falloff for latitude, can be Linear or None
    latFalloffType = Linear
  }

}


// Dynasty
// =============
SPACEDUST_RESOURCE
{
  resourceName = Antimatter
  body = Fury
  RESOURCEBAND
  {
    name = furyRing
    title = #LOC_SpaceDust_Band_LowBelt

    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
       minAbundance = 4.1E-31
       maxAbundance = 1.2E-30


    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 650000
    // minimum altitude for a distribution
    altLowerBound = 550000
    // peak of a distribution
    altPeak = 600000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 10
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear
    altitudeSquish = 0.9
    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 40
    // Minimum latitude for a distribution
    latLowerBound = -40
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 1
    // Falloff for latitude, can be Linear or None
    latFalloffType = Linear
  }

}


// Janus
// =============

SPACEDUST_RESOURCE
{
  resourceName = Antimatter
  body = Janus

  RESOURCEBAND
  {
    name = joolExo
    title = #LOC_SpaceDust_Band_LowBelt

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.000000000000000000000000000000061
    maxAbundance = 0.000000000000000000000000000000421

    // Visuals Data
    // -------------
    countScale = 2
    rotateRate = 1
    
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 600000
    // minimum altitude for a distribution
    altLowerBound = 400000
    // peak of a distribution
    altPeak = 500000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 25
    // Minimum latitude for a distribution
    latLowerBound = -25
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 6
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }

  RESOURCEBAND
  {
    name = tauAreaRing

    title = #LOC_SpaceDust_Band_Ring

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.00000000000000000000000000000011
    maxAbundance = 0.00000000000000000000000000000041

     // Visuals Data
    // -------------
    countScale = 20
    rotateRate = 0.25

    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 23000000
    // minimum altitude for a distribution
    altLowerBound = 21000000
    // peak of a distribution
    altPeak = 22000000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 2
    // Minimum latitude for a distribution
    latLowerBound = -2
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }

}


// Tau
// =========

SPACEDUST_RESOURCE
{
  resourceName = Antimatter
  body = Tau
  RESOURCEBAND
  {
    name = tauRing
    title = #LOC_SpaceDust_Band_LowBelt

    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.00000000000000000000000000000041
    maxAbundance = 0.00000000000000000000000000000121

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 2550000
    // minimum altitude for a distribution
    altLowerBound = 500000
    // peak of a distribution
    altPeak = 600000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 10
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear
    altitudeSquish = 0.9
    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 40
    // Minimum latitude for a distribution
    latLowerBound = -40
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 1
    // Falloff for latitude, can be Linear or None
    latFalloffType = Linear
  }

}


// Eterna
// =============
SPACEDUST_RESOURCE
{
  resourceName = Antimatter
  body = Eterna
  RESOURCEBAND
  {
    name = etarnaRing
    title = #LOC_SpaceDust_Band_LowBelt

    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.000000000000000000000000000000043
    maxAbundance = 0.000000000000000000000000000000120


    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 2500000
    // minimum altitude for a distribution
    altLowerBound = 350000
    // peak of a distribution
    altPeak = 500000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 10
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear
    altitudeSquish = 0.9
    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 40
    // Minimum latitude for a distribution
    latLowerBound = -40
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 1
    // Falloff for latitude, can be Linear or None
    latFalloffType = Linear
  }

}


// Verna
// =============
SPACEDUST_RESOURCE
{
  resourceName = Antimatter
  body = Verna
  RESOURCEBAND
  {
    name = vernaRing
    title = #LOC_SpaceDust_Band_LowBelt

    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
       minAbundance = 1.6E-31
       maxAbundance = 0.5E-30


    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 450000
    // minimum altitude for a distribution
    altLowerBound = 350000
    // peak of a distribution
    altPeak = 400000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 10
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear
    altitudeSquish = 0.9
    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 40
    // Minimum latitude for a distribution
    latLowerBound = -40
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 1
    // Falloff for latitude, can be Linear or None
    latFalloffType = Linear
  }

}